fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
# Required by pydantic's EmailStr, used for the email fields on the auth and
# admin request models. pydantic does not pull this in on its own -- without
# it every `from app.main import app` raises ImportError at startup.
email-validator>=2.1.0
python-dotenv==1.0.1
pytest==8.3.4
# Without this, pytest does not run `async def` tests -- it SKIPS them and
# still reports the run as passing. tests/test_votes.py (the regression test
# for the flagged-vote crash) was silently inert for exactly this reason.
pytest-asyncio==0.24.0
httpx==0.28.1
motor==3.6.0
python-multipart==0.0.18
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
pymongo>=4.6.0
requests>=2.31.0
openpyxl>=3.1.0
# GTFS-realtime protobuf definitions (google.transit.gtfs_realtime_pb2), used
# to publish the service-alert feed in app/gtfs/realtime.py. Pulls in protobuf.
gtfs-realtime-bindings==2.2.0
